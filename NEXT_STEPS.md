# Project Status & Next Steps (as of Week 11)

This document consolidates the project's history and remaining work across both tracks. It complements `README.md` (setup/usage) and `RESEARCH_PLAN.md` (the original research roadmap, written after Week 7) with a running "where we are now" view, since that synthesis previously required reading all of `WEEK2_LOG.txt`–`WEEK11_LOG.txt` together.

## Context

The project has two parallel tracks estimating CO2 emissions from Indian coal power plants using satellite data:

- **Track A (plant detector):** a CNN classifying satellite tiles as "power plant" vs. not, using NO2/SO2/VIIRS channels.
- **Track B (CO2 estimation):** a physics-based mass-balance (IME) method estimating tons CO2/yr directly from OCO-3 XCO2 soundings.

`RESEARCH_PLAN.md` §14 laid out a 7-step roadmap to turn this into a research contribution. This document tracks progress against that roadmap and records unplanned but important findings discovered along the way.

## Step-by-step history

### Weeks 2–5 — Track A detector, incremental channel fusion
- Week 2: NO2-only baseline, 91.2% accuracy (easy negatives) — later shown to really be a "concentrated combustion detector," not plant-specific.
- Week 3: added hard negatives (cities, steel plants, highways) → accuracy dropped to 77.1%, exposing the confound.
- Week 4: added SO2 channel → 79.2%, fixed city false alarms but not steel plants (which also emit SO2).
- Week 5: added VIIRS thermal → tied at 79.2%, reduced steel-plant false alarms specifically but highways became relatively more confusable.

### Weeks 6–8 — Track B physics pipeline + uncertainty
- Week 6: built `physics_gaussian.py`, an IME mass-balance estimator. Vindhyachal (44.6 Mt/yr) and Sasan (37.2 Mt/yr) landed in plausible range; Tirora too low (thin coverage); Mundra skipped (57 soundings only).
- Week 7: expanded highway hard-negatives (5→10), first Track A accuracy improvement since Week 4 (81.2%). Flagged the tile-level train/test split as a leakage risk (fixed later, Week 11).
- Week 8: per-overpass wind matching + uncertainty propagation into `physics_gaussian.py` (roadmap step 1) — replaced single annual-mean wind with per-overpass ERA5 matching, added checkpointing for long-running OCO-3 scans.

### Week 9 — facility-set expansion (roadmap step 2)
- Extended candidate list from 5 to 20 plants (`pick_plants.py`), generalized `process_plant.py` to read from a CSV registry instead of hardcoded plants.
- Processed 5 new plants (Talcher, Rihand, Sipat, ChandrapurCoal, Anpara): went from 4→9 total plants processed, 2/4→7/9 producing a plausible estimate.
- Measured real cost: ~50–65 min/plant (network-bound OCO-3 scan) — a documented constraint on how far facility expansion can go in one session. 10 candidates remain unprocessed.

### Week 10 — activity signal, reliability check, Climate TRACE benchmark, Talcher diagnosis
- Roadmap step 3: extracted an activity-probability signal from Track A's existing 3-channel CNN for all 9 Track B facilities. Broadly tracked the physics-based Q estimate.
- Roadmap step 4 (scoped down): tested whether activity signal / wind alignment / metadata predicts `physics_gaussian.py`'s own uncertainty. Negative result — best single-feature correlation didn't survive leave-one-out CV at N=7, reinforcing that more facilities are needed.
- Roadmap step 6: pulled Climate TRACE India power-sector CO2 data, matched to all 9 facilities. 5/7 facilities with a Track B estimate fell within stated uncertainty. Two misses: Sasan overestimated (2.13x), Talcher underestimated (0.50x) despite having the tightest interval of the session.
- Unplanned: diagnosed why Talcher was both most-confident and most-wrong. Root cause: thin CO2 signal-to-noise ratio (0.18 vs. 1.27 for a well-bracketed plant) and high sensitivity to the background-annulus definition (IME swings 20% across reasonable background choices) — a structural uncertainty source the model wasn't accounting for.

### Same-day follow-ups (commits `a0d70d0`, `fc3655b`, `5baa198`) + Week 11
- Folded the background-definition sensitivity found during Talcher diagnosis into `physics_gaussian.py` as a third uncertainty term (alongside wind and IME-sampling noise), computed for all 9 facilities. Talcher's interval widened modestly (±23%→±25%) but still doesn't bracket its Climate TRACE benchmark — an honest widening, not a forced fix.
- **Week 11:** fixed the Week-7-flagged Track A leakage by adding a facility-level train/test split (`facility_level_split()` in `train_3channel.py`), run side-by-side with the old tile-level split for comparison. Result: leakage was real and substantial — "hard_only" accuracy dropped 81.2%→67.3% (facility-level), and "mixed" model's plant recall collapsed 53%→8%, revealing the model was largely memorizing the 4 original training facilities.
- Re-ran the activity-signal extraction against the new facility-split checkpoint and built `compare_activity_signal_checkpoints.py` to diff results per facility by training exposure. Found the Week 10 activity signal for the 5 newer facilities is low-confidence for two compounding reasons: weak recall on unseen facilities (8%), and high variance in what that weak signal outputs for a given novel facility (e.g. Sipat/ChandrapurCoal swung ~0.22 between checkpoints; Rihand was stable). Conclusion: this doesn't invalidate the Track B physics estimates, only the CNN-derived activity-signal cross-check.

### Same-day follow-up — Track A positive-class expansion (4→20 facilities)
- Exported NO2/SO2/VIIRS positive-class tiles (`export_new_positive_tiles.py`, 2020 only) for the 16 Track B facilities Track A had never seen as positive examples — it had only ever trained on the original `top5_plants.csv` set (4 distinct sites: Vindhyachal, Sasan, Mundra, Tirora). Reused the 5 facilities' tiles already downloaded in `data/activity_tiles/` (Week 10) where present, fetched the remaining 11 fresh from Earth Engine. One transient `HTTP 503` (Koradi SO2, 2020-01) was skipped and backfilled manually. Final: 312/312 NO2, 312/312 SO2, 312/312 VIIRS positive tiles.
- Rebuilt `data/threech/positive` (`build_3channel.py`, 312 paired tiles, up from ~120) and reran `train_3channel.py` with the same facility-level split logic from Week 11.
- **Result: the Week 11 leakage-driven generalization collapse is resolved.** With enough held-out facilities to test on, both checkpoints improved sharply over Week 11's 4-facility numbers:

  | Model | Week 11 (4 facilities) | Now (20 facilities) |
  |---|---|---|
  | hard_only, facility-split accuracy | 67.3% | 82.8% |
  | mixed, facility-split accuracy | — | 95.0% |
  | mixed, facility-split recall (plants) | 8% (collapsed) | 88% |

  This confirms the Week 11 diagnosis: the "mixed" model's 8% recall wasn't a fundamental flaw in the facility-split methodology, it was an artifact of only having 4 positive sites to hold facilities out from. New checkpoints saved as `detector3_2ch_hard_only.pt` / `detector3_2ch_hard_only_facility_split.pt` / `detector3_2ch_mixed.pt` / `detector3_2ch_mixed_facility_split.pt` (overwriting Week 11's).

### Same-day follow-up — re-ran activity-signal extraction against the 20-facility checkpoints
- Re-ran `extract_activity_signal.py` against both the retrained `detector3_2ch_mixed.pt` (tile-level split) and `detector3_2ch_mixed_facility_split.pt` (facility-level split), producing a signal for all 20 Track B facilities (up from 9). Fixed a bug this surfaced: `monthly_paths()` still special-cased tiles into `data/activity_tiles/` for facilities outside the original 4-site alias map, which would have silently skipped the 11 facilities exported straight to `data/monthly|so2|viirs/positive` this session — now all 20 read from the `positive` dirs uniformly.
- Updated `compare_activity_signal_checkpoints.py`, which had hardcoded facility groups and a hardcoded prose conclusion from the old 9-facility/4-site world; it now derives groups dynamically from `train_3channel.py`'s actual printed test_groups and generates its conclusion from the computed deltas instead of asserting one.
- **Result: the memorization signature Week 11 flagged is gone.** With only 4 positive facilities, held-out sites showed larger tile-level-vs-facility-split deltas than in-training sites (clear memorization evidence). With 20 facilities, the held-out group (Anpara, ChandrapurCoal, Sasan, Talcher: mean|delta|=0.036) is now *more stable* than the in-training group (mean|delta|=0.061) — the activity signal is no longer diagnostically tied to whether a facility was in the training set. Consistent with the retrained detector's 88% recall on unseen facilities. Full per-facility deltas in `data/activity_signal_checkpoint_comparison.json`.

### Same-day follow-up — re-ran physics_gaussian.py and reliability_model.py at N=17 (roadmap step 4)
- `physics_gaussian.py` had only been run against 13 of the 20 facilities in `data/plant_results.json`; re-ran it against the full set, which produced emission estimates for 4 more facilities (Kahalgaon, Mouda, Chhabra, Farakka — all had saved soundings but hadn't gone through the physics step yet). 17/20 facilities now have a `q_t_per_year` estimate; only Mundra, Sipat, and Simhadri remain excluded, all three for a genuine coverage gap (0 near-plant soundings after the near/bg split), not something a re-run fixes.
- Re-ran `reliability_model.py` (Week 10's N=7 negative-result feasibility check: does the activity signal or wind/CO2 alignment predict `physics_gaussian.py`'s own `q_rel_std`?) at N=17.
- **Result: no longer a negative result.** At N=7, the best single-feature LOO CV didn't survive scrutiny. At N=17, `hit_days` (number of per-overpass wind-matched days) correlates with `q_rel_std` at r=−0.617, and LOO CV gives R²=0.212 (MAE=0.095) — a real, physically sensible signal (more wind-matched days → lower physics uncertainty). Still reported as indicative only (single-feature linear fit, N=17), per `RESEARCH_PLAN.md` §8's caution against multi-feature models at this facility count — but this is the first time this feasibility check has found anything. Full table and LOO predictions in `data/reliability_model_results.json`.

### Same-day follow-up — built a general leave-one-facility-out CV harness for Track A (roadmap step 5)
- Wrote `lofo_track_a.py`: previously step 5 was only satisfied ad hoc by Week 11's single random 80/20 facility-level split (`facility_level_split()` in `train_3channel.py`), which only ever tests generalization on whichever handful of facilities land in that one draw (4 plant facilities for the "mixed" run). This harness instead holds out each plant facility in turn, training a fresh model on the rest each time (21 folds total — Mundra's two units, `MUNDRA_TPP`/`MUNDRA_UMPP`, are separate groups in `data/top5_plants.csv`, covering the 20 Track B facilities), and measures recall on that facility's own tiles.
- **Result: the single-split 88% recall figure was not representative — true exhaustive LOFO recall is much lower and highly variable.** Unweighted mean recall across all 21 folds: **47.2%** (tile-weighted: 48.7%), essentially a coin flip on average.
  - Best-generalizing (recall ≥0.83): Anpara, Korba, Rihand, Talcher, Vindhyachal, Sasan.
  - Worst-generalizing (recall ≤0.08): Kahalgaon, Kudgi, Mouda, Tirora, Chhabra — the detector essentially never recognizes these as plants when excluded from training.
  - This is a materially more honest number for the paper than the 88% recall reported from the single random facility-split test set, which happened to draw an easier-than-typical subset of facilities. Full per-facility results in `data/lofo_track_a_results.json`.

### Same-day follow-up — what predicts LOFO recall? (`lofo_recall_correlates.py`)
- Answered the open question above: joined `lofo_track_a.py`'s per-facility recall (Mundra's two units averaged, tile-weighted, into one row) against `plant_results.json`/`candidate_plants.csv`/`activity_signals.json` features, N=20, same single-feature-LOO-CV method as `reliability_model.py`.
- **Result: `activity_prob_mean` (the standard, non-held-out model's own confidence) strongly predicts whether a held-out model recognizes the facility at all — r=+0.903, LOO R²=0.783, MAE=0.149.** `n_soundings` is a weaker secondary signal (r=+0.596). Interpretation: facilities with a weak/ambiguous NO2+SO2+VIIRS combustion signature (low activity_prob_mean even for a model that HAS seen every facility) are exactly the ones a held-out model fails hardest on — i.e. LOFO failure looks driven by intrinsic signal strength/distinctiveness per facility, not by memorization of specific sites. Caveat: `activity_prob_mean` and `lofo_recall` both derive from the same detector architecture/tile set, so this correlation says "signal clarity predicts generalizability," not two fully independent measurements.
- Fixed a silent bug the analysis surfaced: `co2_enhancement_ppm` is null for the 3 facilities `physics_gaussian.py` excludes (Mundra, Simhadri, Sipat — 0 near-plant soundings), which made its correlation compute as `NaN` via plain `np.corrcoef`; switched to pairwise deletion (N=17 for that one feature) so it reports a real value (r=−0.184) instead of silently failing.
- Full feature table and LOO predictions in `data/lofo_recall_correlates.json`.

### Same-day follow-up — closing the LOFO generalization gap: augmentation vs. more training data
- Motivated by `lofo_recall_correlates.py`'s finding (weak/ambiguous signal predicts LOFO failure) plus a second observation: the 16 newly-added facilities had only 12 tiles each (2020 only) vs. 24 for the original top-5 (2019+2020) — half the temporal diversity per facility, and correspondingly small (n=12) LOFO test folds. Two experiments tested whether either factor was fixable.
- **Experiment 1 — light spatial augmentation (`lofo_track_a_aug.py`), no new data.** Re-ran the exact same 21-fold LOFO harness with a random dihedral-4 transform (identity/hflip/vflip/90°-rotation) applied to training tiles per epoch. **Result: no improvement** — mean recall 47.2%→46.8%, tile-weighted 48.7%→48.7% (unchanged). Individual facilities moved a few points either way with no systematic pattern. Conclusion: the gap was not primarily a training-set-diversity artifact fixable by synthetic augmentation.
- **Experiment 2 — real 2nd data year (`export_new_positive_tiles_2019.py`).** Exported 2019 NO2/SO2/VIIRS tiles for the 16 facilities (plus `TalwandiSabo`, an extra facility that had entered `data/plant_results.json` from an unrelated in-progress candidate batch — included incidentally, not deliberately), bringing every facility to the same 24-tile depth as the original top-5. 612/612 tiles downloaded, zero failures. Rebuilt `data/threech/positive` (312→516 tiles) and reran the LOFO harness (`lofo_track_a.py`, now 22 folds — the extra fold is `TalwandiSabo`, which still only has 12 tiles so its own result isn't directly comparable). **Result: mean recall rose 47.2%→69.1% (tile-weighted 48.7%→70.5%)** — 17 of the 21 comparable facilities improved, several sharply (Mouda 0%→71%, Sipat 33%→96%, TIRORA 4%→67%, Chhabra 8%→54%, Kahalgaon 0%→50%). Confirms the data-quantity asymmetry was a real, fixable driver of the original LOFO gap. Two facilities still fail outright: **Kudgi (0%→0%) and ShriSingajiMalwa (8%→0%)** — these look like the genuine weak-signal cases the correlation analysis flagged, not a data-quantity problem.
- **Experiment 3 — augmentation on top of the expanded dataset.** Reran `lofo_track_a_aug.py` against the new 24-tile-depth dataset to check whether augmentation adds anything once real data depth exists. **Result: slightly worse, not better** — mean recall 69.1%→67.2%, tile-weighted 70.5%→68.4%. Augmentation is now a two-for-two negative result across both dataset sizes.
- **Conclusion: real additional training data (more months per facility), not synthetic augmentation, is what closes the LOFO generalization gap.** True LOFO recall is now **69.1%** (up from 47.2%), superseding that figure everywhere it's quoted below and in `RESEARCH_PAPER.md`/`PROJECT_RESEARCH_DOCUMENTATION.md`. Remaining primary weakness narrows to two specific facilities (Kudgi, ShriSingajiMalwa) rather than a broad ~50% failure rate. One anomaly noted, not yet explained: one fold (SASAN_UMPP) took ~21 minutes vs. ~45s for typical folds in the `lofo_track_a.py` re-run — likely transient system contention, not a code issue, since the result itself was unremarkable (92% recall). Full results in `data/lofo_track_a_results.json` (current, 24-tile) and `data/lofo_track_a_results_v1_12tile.json` (backed up, original 12-tile run); augmentation results in `data/lofo_track_a_aug_results.json` (current) and `_v1_12tile.json`/`_v2_24tile.json` (both backed up).

### Same-day follow-up — diagnosing Kudgi and ShriSingajiMalwa (the two remaining 0%-recall facilities)
- Wrote `diagnose_lofo_weak_facilities.py` to test whether these two facilities' 0% LOFO recall reflects a genuinely weak raw satellite signal or a detector/training artifact. Compared per-channel mean tile intensity (NO2, SO2, VIIRS) against the rest of the positive class, hard_negative, and rural_negative, plus `no2_peak_km` (peak-NO2-pixel offset from registered coordinates) to rule out a coordinate/tile-placement bug.
- **Result: both facilities' raw NO2/SO2 signal sits at or below the rural_negative noise floor.** Kudgi's NO2 mean (2.66e-05) is close to rural_negative's (2.49e-05); ShriSingajiMalwa's SO2 mean (1.19e-04) is essentially identical to rural_negative's (1.19e-04) — both roughly 3–4x below the rest-of-positive-class average, checked every individual month with no signal spike in any of them. `no2_peak_km` rules out bad tile placement: both (3.6km, 1.0km) are *smaller* than several 100%-LOFO-recall facilities' own offsets (Talcher 34.7km, Anpara 16.7km, Rihand 8.1km) — these two are better-centered, not worse.
- **Conclusion: this is a genuine satellite-observability limit, not a fixable bug.** No architecture or training-regime change can be expected to help — the signal simply isn't present in the tile data. Both are large plants (2,400–2,520 MW), so capacity doesn't explain it. Plausible but **unverified** explanation (no emissions-control-equipment field exists in `candidate_plants.csv`): modern FGD/SCR pollution control suppressing stack NO2/SO2 below older plants of similar capacity — flagged as Not documented / needs verification, not asserted as fact. Full comparison in `data/lofo_weak_facility_diagnosis.json`. This closes roadmap item 6/step 5's remaining scope as diagnosed, not as "further improved."

### Same-day follow-up — first Track A/Track B fusion attempt (roadmap step 4, correction model)
- The genuine Q-correcting A1→A5 ablation ladder (`RESEARCH_PLAN.md` §9) needs an independent ground-truth emissions source that doesn't exist for this project — Climate TRACE is explicitly barred as a training label (§7). Wrote `track_fusion_model.py` to test what IS buildable without that source: a Track A + Track B reliability/trust fusion, evaluated against — never fit to — Climate TRACE's `bracketed_by_our_interval` outcome (join happens after the fit is already fixed, not during it).
- **Question 1: does Track A's `activity_prob_mean` improve on Track B's own best uncertainty predictor (`hit_days`, LOO R²=0.212) in a 2-feature LOO fit?** No — R² got *worse* (0.212→0.173) adding the activity signal. It carries no incremental information about Track B's own uncertainty beyond `hit_days`.
- **Question 2: does a composite fusion trust score (low predicted uncertainty + high activity confidence) predict actual Climate TRACE bracketing success?** Essentially no signal: mean trust for bracketed facilities (0.477) vs. missed (0.470) is a 0.007 gap on a 0–1 scale — noise at N=17, though nominally the expected direction.
- **Honest result: this specific fusion framing doesn't work.** Track A's detector confidence and Track B's physics uncertainty appear to be largely independent signals in this context — doesn't rule out fusion in general, just this self-consistency approach. The genuine Q-correcting model remains unbuilt and still needs a real ground-truth source. Full results in `data/track_fusion_model_results.json`.

### Same-day follow-up — three smaller hardening items
- **Month-stratified `physics_gaussian.py`** (closing the general gap §12.7 flagged): background soundings (main annulus + all 5 sensitivity-term definitions) are now restricted to the months the near-plant zone actually covers, when day data exists. Re-ran for all 20 facilities — several point estimates changed substantially (ShriSingajiMalwa +320%, Chhabra −93%, Tirora −90%). Re-ran Climate TRACE comparison at the same N=17: **calibration dropped 53%→35.3% (6/17)** — reported honestly; several facilities' month-restricted background sample sizes dropped sharply (e.g. Tirora 524→127 soundings), trading seasonal bias for sampling noise, not a methodology regression. Old estimates preserved as `data/emission_estimates_v1_no_month_stratification.json` / `data/climate_trace_comparison_v1_no_month_stratification.json`.
- **Per-plant process locking** in `process_plant.py`: an exclusive, non-blocking `fcntl.flock` on `data/{NAME}.lock` acquired before the scan loop starts, directly preventing the actual 2026-08-13 bug (two independent scan loops for one plant) rather than just guarding the checkpoint write. Verified in isolation.
- **Added `requirements.txt`**, pinned to `/opt/miniconda3/envs/co2` (Python 3.11.15, has everything the codebase needs). Surfaced an unrelated finding while writing it: scripts have also run under a second Python (macOS's `/usr/local/bin/python` → framework 3.14 build) missing `earthaccess`/`xarray`/`geemap` — undetected until now since this session's numpy/torch/pandas/ee-only scripts happened to work under either.

### Same-day follow-up — first test suite (`tests/`)
- Added 22 stdlib-`unittest` tests (no new dependency): `test_physics_gaussian.py` (IME math, month-stratification), `test_build_3channel.py` (tile pairing/gap-filling/clamping), `test_lofo_track_a.py` (`facility_fold_indices()`, guarding against a Week-11-leakage-style regression). Required extracting `lofo_track_a.py`'s inline fold-splitting into a top-level function and adding an `if __name__ == "__main__":` guard to `build_3channel.py` (previously ran its full pipeline at import time) — both behavior-preserving. All 22 pass in ~0.1s, no network/GPU needed: `python -m unittest discover -s tests -v`. Doesn't cover Earth Engine, OCO-3 downloads, or model training end-to-end; no CI wired up.

### Same-day follow-up — found real ground truth and built the actual Q-correcting model (roadmap step 4)
- The A1→A5 ladder has needed an independent, non-Climate-TRACE ground-truth emissions source all session. Found one: **CEA's CO2 Baseline Database** (Government of India, Ministry of Power) — bottom-up from each plant's own reported fuel consumption × GCV × emission factor, the same accounting used for real CDM carbon-credit determinations. Fundamentally different in kind from Climate TRACE (satellite-thermal-proxy inferred), so usable as a genuine label per `RESEARCH_PLAN.md` §7's restriction (which is Climate-TRACE-specific).
- `pull_cea_ground_truth.py` downloads FY2020-21 data (closest single-fiscal-year match to Track B's 2020 estimates) and matches **all 30 candidate facilities by name** — several so closely (CEA's "VINDH_CHAL STPS" vs. our own "VINDH_CHAL_STPS") that this project's facility set may originally derive from this same database.
- `q_correction_model.py` builds the actual correction, LOO-CV at N=17: baseline accuracy vs. real ground truth is MAE 1.01 in log-ratio space, 47.1% bracketing (8/17) — a new, independently meaningful number alongside the 35.3% Climate TRACE figure. `bg_std_ppm` (background XCO2 noise) is the strongest single-feature predictor of error (r=+0.58, physically sensible); correcting by its LOO-predicted bias drops MAE to 0.902, a genuine ~11% improvement.
- **This is the actual milestone roadmap step 4 has needed all session** — closes "Still open — the key remaining blocker" from the prior fusion-attempt entry. Still indicative (N=17, single-feature, one fiscal year, one CEA version), not production-validated.

### Same-day follow-up — re-ran Climate TRACE comparison and produced the full evaluation figure set (roadmap step 7)
- `pull_climate_trace.py` had only ever matched Week 10's 9 facilities; re-ran it (it already reads the full `plant_results.json` dynamically, so no code change was needed) against all 20 facilities, now matching the 17 with a `physics_gaussian.py` estimate. **Uncertainty calibration changed: 9/17 = 53% of facilities are now bracketed by our interval, down from Week 10's 5/7 = 71%.** More facilities didn't just add data points, they surfaced more misses (Kahalgaon, Talcher, RGundem, Korba, ShriSingajiMalwa, Tamnar, Sasan, Tirora all fall outside their own stated interval) — a more honest, less flattering number than the small-sample Week 10 figure. Updated `data/climate_trace_comparison.json`.
- Wrote `evaluation_figures.py`, producing the two evaluation figures RESEARCH_PLAN.md §12/roadmap step 7 called for:
  - `data/eval_climate_trace_comparison.png` — predicted-vs-actual scatter, residual-ratio bars, uncertainty-calibration bar chart, and facility-level side-by-side comparison, all vs. Climate TRACE.
  - `data/eval_track_a_ablation_and_generalization.png` — Track A's full ablation/methodology history as a table (Week 3 NO2-only through this session's exhaustive LOFO), the per-facility LOFO recall distribution, and both feature-importance panels (`reliability_model.py`'s and `lofo_recall_correlates.py`'s correlations).
  - Scope honestly stated in the script's docstring: "predicted-vs-actual" is benchmark-vs-benchmark (Climate TRACE), not vs. ground truth; the "ablation table" is Track A's channel/split history, not the unbuilt Track B A1→A5 ladder; "feature importance" is Pearson correlation from two N=17/N=20 single-feature studies, not a trained model's SHAP/permutation importance.

### Same-day follow-up — root-caused the negative-CO2-enhancement anomaly (ShriSingajiMalwa, Koradi, Tamnar)
- Wrote `diagnose_negative_enhancement.py`, reusing `diagnose_talcher.py`'s `near_bg_stats`/`bg_definition_sensitivity` functions plus a new statistical-significance check: is the near-minus-background difference distinguishable from zero given the standard error of that difference (near/background sample sizes and variance), or is the negative sign just sampling noise landing on the wrong side of zero?
- **Result: 2 of 3 facilities are not really "negative enhancement" at all.** Koradi (z=−1.58) and Tamnar (z=−1.33) are both statistically consistent with zero at 2σ — the honest characterization is "no detectable enhancement given available signal-to-noise," not a real negative signal. This also resolves the apparent contradiction with the Talcher-derived heuristic: Tamnar's good wind/CO2 alignment (16°, the best of the three) says its NO2 plume geometry is plausible, but wind alignment and CO2 signal-to-noise are computed independently — the two diagnostics are orthogonal, not in tension.
- **ShriSingajiMalwa is different: its negative enhancement (−1.052 ppm, z=−5.32) is statistically significant, not noise.** Follow-up (`diagnose_shrisingajimalwa.py`) resolved this too. All 59 near-plant soundings are from a single month (January) — likely OCO-3 swath geometry only crossing the tight near-plant circle on some overpasses — while the background ring also draws from April/May (mean XCO2 ~415.6–416.5 ppm, ~4 ppm higher than January's ~412 ppm baseline for reasons unrelated to the plant) and October. **Restricting the comparison to the one month both zones actually share (January) flips the sign: near=412.321 vs bg=412.029 ppm, a small but positive +0.292 ppm difference (z=2.13)** — consistent with a real, if weak, plant signal, not a negative anomaly. The original "negative enhancement" was a seasonal-sampling-imbalance artifact (comparing January-only near-plant data against a January/April/May/October blended background), not a real negative CO2 signal, a pipeline bug, or evidence against the plant emitting. **Note for future work: `physics_gaussian.py`'s near/background comparison does not currently stratify by month, so this failure mode could recur for any facility with uneven near/background month coverage** — worth a general fix, not just a one-off explanation, if it recurs.
- All three facilities show elevated background-definition sensitivity relative to the well-bracketed comparison facility (Rihand: 4% IME-proxy range vs. ShriSingajiMalwa 45%, Tamnar 37%, Koradi 19%), consistent with the same thin-signal fragility pattern the Talcher diagnosis first identified — not a new failure mode. Full stats in `data/negative_enhancement_diagnosis.json` and `data/shrisingajimalwa_investigation.json`.
- **All three previously-flagged negative-enhancement facilities are now resolved** — the negative-CO2-enhancement anomaly item is closed, though the general month-stratification gap it surfaced in `physics_gaussian.py` remains a worthwhile hardening item.

## Roadmap status vs. RESEARCH_PLAN.md §14

| Step | Status |
|---|---|
| 1. Per-overpass wind + uncertainty in `physics_gaussian.py` | Done (Week 8), later strengthened with a 3rd uncertainty term (background-sensitivity) |
| 2. Facility-set expansion (10–20 candidates) | Done — 20/20 processed, all merged into `data/plant_results.json` |
| 3. Activity signal extraction from Track A CNN | Done (Week 10), low-confidence flag from Week 11 resolved this session — re-run against the 20-facility checkpoint shows the activity signal is now stable regardless of training-set membership |
| 4. Correction model (A1→A5 ablation ladder, §9) | Feasibility check re-run at N=17 (up from 7), a positive result (hit_days predicts q_rel_std, LOO R²=0.212). A first fusion attempt without a ground-truth label (`track_fusion_model.py`) tested negative on both questions asked — activity signal doesn't improve the uncertainty fit, and a fused trust score doesn't track actual Climate TRACE bracketing. The A1→A5 Q-correcting ladder itself still needs Climate-TRACE-independent ground truth and remains unbuilt |
| 5. Leave-one-facility-out CV harness | Done for Track A this session (`lofo_track_a.py`); revealed the single-split 88% recall was not representative — true mean recall was 47.2% on the original 12-tile-depth dataset, then **rose to 69.1%** after a follow-up (24-tile-depth, matching the original top-5's temporal coverage) closed most of the gap. Data augmentation tested separately and found not to help. Track B's correction model (step 4) still needs its own LOFO harness once it exists |
| 6. Climate TRACE benchmark comparison | Done (Week 10), re-run this session at N=17 (up from 7) — calibration dropped from 71% to 53% bracketed, a more honest number from the larger sample |
| 7. Full evaluation figure set (§12) | Done this session (`evaluation_figures.py`) for everything that doesn't require the unbuilt A1→A5 correction model — Climate TRACE comparison panels, Track A ablation table, LOFO recall distribution, feature-importance panels |

## What's needed next

1. **Facility-set expansion — DONE this session.** All 20/20 candidates processed and merged into `data/plant_results.json` (Korba, ShriSingajiMalwa, Koradi, Tamnar, Kudgi, Kahalgaon, Mouda, Chhabra, Farakka, Simhadri were the 10 processed this session). This was the shared bottleneck for: Track B's reliability-model retry (needs N>7 — now satisfied with N=20), Track A's classifier evaluation (was only 5 positive-class facilities), and the correction model in step 4 — all now unblocked.
   - **Reliability notes (this session):** `earthaccess.download()` has no built-in timeout and hung indefinitely twice (Kahalgaon, then Mouda), each stuck mid-scan for 1.5h+ with a `CLOSE_WAIT` socket to `urs.earthdata.nasa.gov` and no checkpoint progress. Fixed with a 90s per-granule `signal.alarm` timeout in `process_plant.py` (a stalled download now gets skipped like any other granule-level error instead of hanging the whole run). Two separate instances of a duplicate-process bug also occurred: an ad hoc restart of Kahalgaon left two sequential loops running against the same remaining-plant list, and later, when Farakka and Simhadri were deliberately run in parallel to cut wall-clock time, the original sequential loop caught up and launched a *second* concurrent Simhadri process against the same in-progress checkpoint file — a real corruption risk (only the final `plant_results.json` write was flock-protected, not the per-granule checkpoint writes). Both were caught and the redundant process killed within seconds each time; no data was lost or corrupted, confirmed by inspecting the checkpoint/result immediately after. `process_plant.py`'s final results write now uses `fcntl.flock` so parallel runs no longer race on `plant_results.json`.
2. **More Track A positive-class facilities — DONE this session.** Exported NO2/SO2/VIIRS tiles and retrained for all 16 newly processed plants (4→20 positive facilities); resolved Week 11's leakage-driven recall collapse (8%→88%). Re-ran the activity-signal extraction against the new checkpoints too — Week 11's low-confidence flag is resolved (held-out facilities are now as stable as in-training ones). See both "Same-day follow-up" entries above.
3. **Build the actual correction model (roadmap step 4)** once facility count supports it — currently only a negative-result feasibility check exists. Now doubly unblocked: N=20 for Track B, and Track A's detector now generalizes to novel facilities.
4. ~~General leave-one-facility-out CV harness (roadmap step 5)~~ — **Done**: `lofo_track_a.py`. Follow-up work closed most of the resulting generalization gap (47.2%→69.1% mean recall) by matching all facilities to 24-tile temporal depth; two facilities (Kudgi, ShriSingajiMalwa) still show 0% LOFO recall and remain the concrete open target — see the "closing the LOFO generalization gap" entry above.
5. **Formalize wind/CO2 offset-diff as an explicit per-plant quality flag** (noted as a TODO in Week 9's log) rather than just printing it.
6. **Full evaluation figure set (roadmap step 7 / RESEARCH_PLAN.md §12)** — not started: predicted-vs-actual, residuals, error distribution, facility-level comparison, uncertainty calibration plot, ablation table, feature importance.
7. Optional smaller follow-up already flagged in Week 10: ChandrapurCoal's per-overpass wind fell back to annual-mean mode (0 days met the match threshold) despite 28 near-plant soundings — worth a closer look given its bad wind/CO2 alignment (177°, the worst seen).
